# mistral-vibe — config for Mistral's `vibe` agentic coding CLI

> **Status:** experiment — Claude Code is the primary AI coding agent. See `docs/repo-map.md`.

- `config.toml` is the single source of truth for `vibe`
