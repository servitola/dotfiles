# eqmac — backup of the eqMac system-audio equalizer preset

- Not symlinked or installed by the Makefile. eqMac keeps live presets in its own app-data store; restore by importing `preset.json` through the eqMac UI.
