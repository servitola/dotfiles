# chromium-vimium-extension — exported Vimium settings for Chromium-based browsers

- `vimium-options.json` is a backup of the Vimium extension's Options page (keyboard nav for the browser): custom key mappings, link-hint characters (`1234qweasd`), exclusion rules (Gmail, dwservice), search engines, and hint CSS
